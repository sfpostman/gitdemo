apiVersion: v1
kind: Service
metadata:
  name: types-service
  namespace: pokedex-demo
  labels:
    app: types
spec:
  type: ClusterIP
  ports:
    - port: 3002
      targetPort: http
      name: http
  selector:
    app: types
