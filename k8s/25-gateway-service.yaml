apiVersion: v1
kind: Service
metadata:
  name: gateway-service
  namespace: pokedex-demo
  labels:
    app: gateway
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      name: http
  selector:
    app: gateway
