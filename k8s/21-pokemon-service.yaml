apiVersion: v1
kind: Service
metadata:
  name: pokemon-service
  namespace: pokedex-demo
  labels:
    app: pokemon
spec:
  type: ClusterIP
  ports:
    - port: 3001
      targetPort: http
      name: http
  selector:
    app: pokemon
