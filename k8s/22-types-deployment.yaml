apiVersion: apps/v1
kind: Deployment
metadata:
  name: types
  namespace: pokedex-demo
  labels:
    app: types
spec:
  replicas: 1
  selector:
    matchLabels:
      app: types
  template:
    metadata:
      labels:
        app: types
    spec:
      containers:
        - name: types
          image: pokedex-types:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 3002
          env:
            - name: MYSQL_HOST
              value: mysql
            - name: MYSQL_USER
              value: root
            - name: MYSQL_DATABASE
              value: pokedex
            - name: MYSQL_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: mysql-secret
                  key: MYSQL_ROOT_PASSWORD
          readinessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 60
            periodSeconds: 15
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
